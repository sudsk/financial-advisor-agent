# k8s/mcp-server-deployment.yaml - CURRENT VERSION IS FINE
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mcp-server
  namespace: financial-advisor
spec:
  replicas: 2
  selector:
    matchLabels:
      app: mcp-server
  template:
    metadata:
      labels:
        app: mcp-server
    spec:
      containers:
      - name: mcp-server
        image: REGION-docker.pkg.dev/PROJECT_ID/financial-advisor/mcp-server:latest
        ports:
        - containerPort: 8080
        env:
        # Remove the wrong BANK_ANTHOS_URL - not needed!
        # The bank_anthos_client.py has hardcoded URLs that work
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi" 
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
